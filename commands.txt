Update GitHub:

git add .
git commit -m "Commit Message"
git push

Other git commands:
# see which files have changes you haven't updated on GitHub
git status

Run file

python3 "file name"

About creating a permanent (local) database:
- Whilst the app is running create a new tab(MUST BE TAB) in the terminal
- Import the db from the app
- Then db.create_all() function must be used in the terminal
https://www.youtube.com/watch?v=hbDRTZarMUw

About sessions: (basically cookies but stored on the server side)
https://flask.palletsprojects.com/en/2.3.x/quickstart/#sessions

About the flask-login module:
https://flask-login.readthedocs.io/en/latest/

More about how the aunthetication works:
https://www.digitalocean.com/community/tutorials/how-to-add-authentication-to-your-app-with-flask-login

div{
  width: 340px;
  padding: 10px;
  border: 10px solid green;
  margin: 0;
} # RIP green box

def create_graph(self):
      graph = pygal.Line()
      graph.title = "Test Day graph"
      graph.x_labels = self.get_x_axis_values()
      graph.add = ('Test', [5,6,3,2,8])
      graph_data = graph.render_data_uri()
      return graph_data

      day_calories_consumed = db.session.query(func.sum(MealRecord.calories)).group_by(MealRecord.user_id, MealRecord.date_created).having(MealRecord.user_id == currentuser and MealRecord.date_created == date).scalar()

      if day_calories_consumed == None:
          return 0
Healthtracker gmail app password
qong smln fclv seho
